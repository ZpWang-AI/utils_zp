# Example Workspace Entry

说明：示例 workspace 只演示结构，不代表真实环境。

## 建议结构

- `example` (`ex`)
  入口：`~/example/.agent/README.agent.md`

## 示例写法

- 在这里补充该 workspace 的 repo 入口、环境限制和临时目录约定
- 如果某些命令只能在特定机器、容器或账号下执行，也应在这里明确写出
- 如果该 workspace 有额外的文档、日志或落盘目录约束，也统一写在这里
